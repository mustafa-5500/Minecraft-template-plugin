This is the Documentation folder, recreate the plugin's file structure, however replace each code file with a corresponding .md file for documentation. The main documentation goes over the function signatures and their inputs and outputs. For more detailed documentation going over the implementation of the functions, create seperate Software Detailed Documentation (SDD) .md files. Add the _SDD suffix to their file name.