# Auto-detect text files and normalise line endings to LF
* text=auto eol=lf

# Force Unix line endings on shell scripts (critical for CI)
gradlew text eol=lf
*.sh    text eol=lf

# Force Windows line endings on batch files
*.bat   text eol=crlf

# Binary files
*.jar   binary
